services:
  - type: web
    name: pdas-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn model_service.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
